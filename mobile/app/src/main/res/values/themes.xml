<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Base application theme.
        Compose handles its own theming, so this XML theme is kept minimal —
        just status bar / navigation bar colours and the parent for the activity window.
    -->
    <style name="Theme.TradersGuardian" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Status bar transparent; Compose draws into it via enableEdgeToEdge() -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@color/bg_dark</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowBackground">@color/bg_dark</item>
    </style>
</resources>