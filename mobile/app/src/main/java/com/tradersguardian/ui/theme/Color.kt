package com.tradersguardian.ui.theme

import androidx.compose.ui.graphics.Color

// ── Background ────────────────────────────────────────────────────────────────
val BgDark      = Color(0xFF0D1117)   // deepest background
val BgMid       = Color(0xFF111318)   // page background
val Surface     = Color(0xFF161B22)   // card surface
val Surface2    = Color(0xFF1C2128)   // inner card / stat chip
val NavBar      = Color(0xFF0D1117)   // bottom nav bar

// ── Text ──────────────────────────────────────────────────────────────────────
val TextPrimary  = Color(0xFFE6EDF3)
val TextSecondary= Color(0xFFADBBC4)
val TextMuted    = Color(0xFF8B949E)
val TextDisabled = Color(0xFF484F58)

// ── Accent ────────────────────────────────────────────────────────────────────
val AccentCyan    = Color(0xFF00D4FF)   // primary CTA
val AccentCyanDim = Color(0xFF0099BB)   // pressed / dim

// ── Status ────────────────────────────────────────────────────────────────────
val SuccessGreen  = Color(0xFF3FB950)
val SuccessBg     = Color(0x1F3FB950)
val ErrorRed      = Color(0xFFF85149)
val ErrorBg       = Color(0x1FF85149)
val WarningAmber  = Color(0xFFD29922)
val WarningBg     = Color(0x1FD29922)
val PendingBlue   = Color(0xFF58A6FF)
val PendingBg     = Color(0x1F58A6FF)

// ── Border ────────────────────────────────────────────────────────────────────
val BorderColor   = Color(0xFF30363D)
val BorderFocus   = Color(0xFF00D4FF)

// ── Input ─────────────────────────────────────────────────────────────────────
val InputBg       = Color(0xFF0D1117)

// ── Aliases kept for backward-compat ─────────────────────────────────────────
val NavDark       = NavBar
val WarningOrange = WarningAmber